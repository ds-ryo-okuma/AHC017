#include <bits/stdc++.h>

#include "graph.hpp"

using namespace std;

int main() {
    // int V = 5, E = 10;

    // graph g(V, E);

    // g.add_edge(0, 1, 2);
    // g.add_edge(0, 2, 3);
    // g.add_edge(1, 2, 1);
    // g.add_edge(1, 3, 2);
    // g.add_edge(1, 4, 3);
    // g.add_edge(2, 3, 1);
    // g.add_edge(3, 4, 2);
    // g.add_edge(4, 2, 1);
    // g.add_edge(4, 0, 4);
    // g.add_edge(4, 1, 1);

    // g.dijkstra(0);

    // g.backup();
    // g.backup();
    // g.backup();
    // g.backup();
    // g.backup();
    // g.backup();

    // g.dijkstra_add_edge(edge(0, 4, 0));
    // g.dijkstra_remove_edge(edge(0, 4, 0));

    // g.rollback();

    // g.commit();

    // cout << "end" << endl;

    vector<int> vec = {1, 2, 3, 4};
    vec.erase(vec.end());

    return 0;
}